# SiteMultiSelect 全选 / 反选（已废止）

> **状态：废止（2026-08-06）**  
> 产品确认：`SiteMultiSelect` **不需要**全选/反选。全选/反选仅出现在可搜索多选组件 `SearchableMultiSelect`（见 `2026-08-06-dedup-branch-collection-multiselect-design.md`）。

原方案不再实施；若代码中曾加入相关 UI，应回退。
